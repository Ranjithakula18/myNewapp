name: print message 
on:

    push:
      branches: [ "main" ]

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print a message using echo
        run: echo "Hello, GitHub Actions!"

          
